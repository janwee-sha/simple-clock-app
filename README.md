# 此刻 · 圆盘时钟

一个简洁的响应式时钟前端。主页会在视口中央显示随设备本地时间实时转动的圆盘时钟，并附带数字时间与日期。

## 技术栈

- React + TypeScript
- Vite
- 原生 CSS，无 UI 组件库

## 本地运行

需要 Node.js `>=22.12.0`。

```bash
npm install
npm run dev
```

## 生产构建

```bash
npm run build
```

构建产物位于 `dist/`，可直接部署到任意静态网站托管服务。
